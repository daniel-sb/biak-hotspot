# --- secrets: never commit ---
.env
.env.*
!.env.example
*.key
*.pem
credentials.json
service-account*.json
ee-*.json
secrets.yaml
config.local.yaml

# --- generated data ---
# Operational API responses, written fresh by every run. Test inputs live in
# tests/fixtures/ and are tracked; nothing under data/ ever should be.
data/raw/
data/processed/
data/cache/
*.pmtiles
*.tif
*.tiff
*.nc
*.hdf

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/
.pytest_cache/
*.egg-info/

# --- os / editor ---
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# National BIG geodatabase (340 MB, manually downloaded, not redistributable here).
# Derive data/boundaries/ from it with scripts/extract_boundary.sh
*.gdb/
*.gdb.zip

# Himawari raw band-segments (~100 MB/day) - never commit
data/raw/himawari/
